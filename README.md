# RepoGaurd
RepoGuard is a Python-based SAST tool that scans repositories for hardcoded secrets, insecure coding patterns, and security misconfigurations. It uses AST-based analysis to detect risky functions, unsafe deserialization, and command execution risks, with centralized findings management and severity-based reporting.
